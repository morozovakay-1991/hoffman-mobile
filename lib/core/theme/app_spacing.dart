// TODO(figma): replace with the real spacing scale once Figma MCP access is
// restored.
/// Spacing tokens mirrored from `docs/mockups/design-tokens.json` (`spacing`).
abstract final class AppSpacing {
  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
}
